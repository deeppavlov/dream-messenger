services:

  messenger:
    container_name: demo-messenger
    build:
      context: ../../.
      dockerfile: ./Dockerfile
      args:
        MODE: demo
    ports:
      - "6994:80"
    volumes:
      - ../nginx.conf:/etc/nginx/conf.d/default.conf
    deploy:
      resources:
        limits:
          memory: 256M
        reservations:
          memory: 256M

version: '3.7'
